Run "npm i" while in "telemetry-system/" directory to install dependencies.

Run command "npm start" (or "nodemon app.js" if you have nodemon installed globally) while in "telemetry-system/" directory to start the micro-service app.
(Make sure Kafka and Elasticsearch are running locally)